<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitfusion.mapper.TargetRoutineMapper">

    <select id="getLeastUsedParts" parameterType="int" resultType="string">
        select part
        from (
            select ex.category   as part,
                   count(*)     as cnt
            from fitfusion_exercise_logs l, fitfusion_exercises ex
            where l.user_id = #{userId}
            and ex.exercise_id = l.exercise_id
            group by ex.category
            order by cnt asc
        )
        fetch first 3 rows only
    </select>
</mapper>